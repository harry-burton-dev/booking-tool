# REVERT-CONTRACT.md additions (proposed)

Generated by the overnight SAFE fix batch (2026-07-31), theme 1 (markers). Do not apply directly —
this is text for a human/orchestrator to fold into `docs/REVERT-CONTRACT.md` in the entry format
that file already uses. Not wired into `revert-check` until it lands there.

---

### booking-create-home

Home screen series create: same dev/prod shape as booking-create-find and booking-create-rooms —
the marker sits directly above the `Refresh(Book_Bookings)` call so SharePoint-assigned IDs land
in colBookings. Added to close the H-2 gap (code-review 20260731): Home's create site had the
`Refresh(Book_Bookings); ClearCollect(colBookings, Book_Bookings);` twins that Find/Rooms carry,
but no marker and no contract entry — this entry brings it in line with its siblings.

```dev
reinstate Refresh(Book_Bookings) here
```

```prod
Refresh(Book_Bookings);
```

Note: as found in the current sync, Home/Find/Rooms all already carry the live
`Refresh(Book_Bookings);` call (not the commented-out dev placeholder the existing
`booking-create-find`/`booking-create-rooms` entries describe) — the `dev` pattern above is
copied verbatim from those two entries for consistency, but whoever folds this in should confirm
against the current source whether the `dev`/`prod` patterns for all three `booking-create-*`
entries still match reality, since none of the three sites are in the commented-out state the
existing contract text implies.
