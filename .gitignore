# Shallow clone of powerapps-process; carries its own git history
.pa-toolkit/

# Per-developer Claude Code settings
.claude/settings.local.json

# canvas-guard / pac-verify machine-local state
.claude/canvas-guard/state/
.claude/pac-verify/
